# Environment Variables Template for Webhook Relay Service
#
# IMPORTANT: This is a TEMPLATE file. Do NOT commit actual secrets to git.
# Create a real .env.webhook file on production with actual values.
#
# File Location: /proj/redcudi.org/.prod/.env.webhook
# File Permissions: 600 (read-only by owner)
#
# Usage by systemd:
#   The redcudi-webhook-relay.service loads these variables via:
#   EnvironmentFile=/proj/redcudi.org/.prod/.env.webhook
#
# Setup:
#   1. Copy this file: cp .env.webhook.example .env.webhook
#   2. Edit the file: vim .env.webhook
#   3. Add your actual secret values below
#   4. Set permissions: chmod 600 .env.webhook

# GitHub Personal Access Token
# Purpose: Authenticate with GitHub API to trigger repository_dispatch events
# 
# How to get:
#   1. Go to https://github.com/settings/tokens
#   2. Click "Generate new token (classic)"
#   3. Give it a name: "Redcudi Webhook Relay"
#   4. Select scope: "repo" (full control of private repositories)
#   5. Generate and copy the token
#
# Security: This token has access to your repositories. Treat it like a password.
GH_PAT=ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Webhook Authentication Secret
# Purpose: Verify that webhook requests come from Strapi (prevents unauthorized triggers)
#
# How to generate:
#   openssl rand -hex 32
#   OR use any random string you can remember
#
# Must match in:
#   - Strapi webhook configuration (Settings → Webhooks → Headers → x-relay-secret)
#   - Curl test command (if testing manually)
RELAY_SECRET=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Relay Service Port
# Default: 4000 (localhost only, exposed via nginx proxy)
# Usually no need to change this unless port conflicts
PORT=4000
